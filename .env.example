# Copy to .env locally (never commit .env).
# Paste the Cookie request header value from a logged-in browser session.
# Rotate immediately if a cookie was ever pasted into chat or a ticket.
JIQIZHIXIN_COOKIE=
